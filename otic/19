import math

x=float(input( "??? "))

match x:
        case x if x > 0:
            print(math.log2(x) - (x**2))
        case x if -1 <= x <= 0:
            print(math.cos(x) + math.sin(x))
        case x if x < -4:
            print(math.exp(x) + x ** math.e)
            